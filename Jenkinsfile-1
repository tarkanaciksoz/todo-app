pipeline {
    agent {
        label 'mac'
    }
    options {
        buildDiscarder(logRotator(numToKeepStr: '5'))
    }
    stages {
        stage('Testing compose') {
            steps {
                sh '''
                docker --version
                docker compose version
                '''
            }
        }
    }
}